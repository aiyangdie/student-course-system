# Security Policy

## 已做的安全措施

- 管理员密码使用 **BCrypt** 存储与校验（不再默认明文入库）
- 接口除登录外需携带 `Authorization: Bearer <token>`
- Token 具备过期时间（默认 12 小时），过期自动失效
- 统一异常处理，避免随意堆栈直出
- MyBatis-Plus 参数绑定，降低 SQL 注入风险
- 开源仓库不包含本机数据库真实密码（使用 `application-example.yml` / 本地 profile）
- 默认关闭 MyBatis SQL 控制台全量打印，减少日志泄密

## 已知限制（教学演示系统，非生产级）

| 项 | 说明 | 建议 |
|----|------|------|
| Token 存内存 | 多实例不共享，重启失效 | JWT + Redis |
| CORS 较宽松 | 便于本地联调 | 生产限制域名 |
| 单管理员模型 | 无细粒度 RBAC | Spring Security + 角色权限 |
| 物理删除 | 关联数据可能残留 | 软删除 / 外键级联策略 |
| 无接口限流 | 可能被爆破 | 网关限流 + 登录失败锁定 |
| 无操作审计 | 难追溯变更 | 操作日志表 |
| HTTPS | 本地 HTTP | 生产强制 HTTPS |
| 演示账号 | README 含默认账号 | 上线后立即修改 |

## 上报漏洞

若发现安全问题，请通过 GitHub Issues 或仓库所有者邮箱联系，请勿公开传播可利用细节。

## 重要提醒

请勿将数据库密码、云密钥、GitHub Token 提交到仓库或发到聊天工具。若 Token 已泄露，请立即在 GitHub 撤销并重新生成。
